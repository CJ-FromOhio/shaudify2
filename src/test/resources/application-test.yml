spring:
  cache:
    type: redis
  datasource:
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-test.yaml
    enabled: true
#    init:
#      mode: always
#      data-locations: classpath:/sql/data.sql